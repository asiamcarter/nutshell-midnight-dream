import events from "./events"
import data from "./data"

const eventsList = {
    listEvents(){
        // get events from database
        .then(event => {
            event.forEach( )
        })
    }
}

export default eventsList